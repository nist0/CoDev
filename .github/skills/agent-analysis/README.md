# agent-analysis skill

This skill provides scripts and methods for analysing agent files in the repository. Use the scripts in scripts/ for anchor line extraction, contract audits, and skill reference checks.

- `python scripts/agent_anchor_lines.py` -- extract anchor lines

- `python scripts/agent_line_audit.py` -- audit contract structure

- `python scripts/agent_skill_audit.py` -- audit skill references
